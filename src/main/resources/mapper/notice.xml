<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.firstproject.domain.notice.NoticeDao">

    <insert id="insert">
      INSERT INTO employee(NAME, created_at) VALUES(#{name}, NOW());
   </insert>

    <select id="findAll" resultType="site.metacoding.firstproject.domain.notice.Notice">
        SELECT
        notice_title,notice_coname, notice_task,notice_qual,
        notice_position,notice_sal,notice_period
        FROM notice
        ORDER BY notice_period DESC;
    </select>

    <select id="findByJobNotice" resultType="site.metacoding.firstproject.domain.notice.Notice">
        SELECT
        n.notice_title,n.notice_coname, n.noti  ce_image, n.notice_task, n.notice_qual,
        n.notice_position,n.notice_sal, n.notice_period,
        j.job_code
        FROM notice
        INNER JOIN job j
        ON n.job_id = j.job_id
        WEHRE n.job_code = #{jobCode}
        ORDER BY created_at DESC;
    </select>

    <select id="findById" resultType="site.metacoding.firstproject.domain.notice.Notice"></select>

    <update id="update"></update>

    <delete id="deleteById"></delete>


</mapper>