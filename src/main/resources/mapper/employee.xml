<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.firstproject.domain.employee.EmployeeDao">

	<insert id="insert">
		insert into employee(employee_name, employee_birth, employee_sex, employee_username, employee_password, employee_email, employee_tel, employee_location, job_id) 
		VALUES(#{employeeName}, #{employeeBirth}, #{employeeSex}, #{employeeUsername}, #{employeePassword}, #{employeeEmail}, #{employeeTel}, {employeeLocation}, #{jobId});
	</insert>

	<select id="findAll"
		resultType="site.metacoding.firstproject.domain.employee.Employee">
		SELECT *
		FROM employee
	</select>

	<select id="findById"
		resultType="site.metacoding.firstproject.domain.employee.Employee">
		SELECT *
		FROM employee
		WHERE employee_id = #{employeeId}
	</select>

	<update id="update">

	</update>

	<delete id="deleteById">

	</delete>


</mapper>