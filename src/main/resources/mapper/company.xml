<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.firstproject.domain.company.CompanyDao">

	<insert id="insert"></insert>

	<select id="findAll" resultType="site.metacoding.firstproject.domain.company.Company"></select>

	<select id="findById" resultType="site.metacoding.firstproject.domain.company.Company"></select>

	<update id="update">
		UPDATE product 
		SET company_number = #{companyNumber}
		company_name = #{companyName}
		company_email = #{companyEmail}
		company_tel = #{companyTel}
		company_location = #{companyLocation}
		company_username = #{companyUsername}
		company_password = #{companyPassword}
		job_id = #{jobId}
    WHERE company_id = #{companyId}
	</update>

	<delete id="deleteById"></delete>


</mapper>